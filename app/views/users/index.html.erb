<% content_for :secondary, render('user_form') %>

<%= render 'users', :users => @users.select(&:is_employee?), :type => "employees" %>
<%= render 'users', :users => @users.select(&:is_client_user?), :type => "clients' users" %>
