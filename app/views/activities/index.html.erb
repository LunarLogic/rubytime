<% throw_content :secondary, partial(:filter) %>

<h1>Activities</h1>

<% if @activities.empty? %>
  No activities found for selected critieria. 
<% else %>
  Found <%= @activities.size %> matching activities (<%= Activity.count %> total).<br/><br/>
  <% @activities.each do |activity| %>
    <%= activity.project.name %>, 
    <%= activity.date %>: 
    <%= activity.user.name %> 
    (<%= activity.minutes / 60.0 %>h)
    <br/>
  <% end %>
<% end %>