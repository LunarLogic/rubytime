<% if current_user.can_see_clients? %>
  <%= render 'activities/grouped_clients', :clients => unique_clients_from(@activities) %>
<% else %>
  <%= render 'activities/grouped_projects',
        :projects => unique_projects_from(@activities, current_user.client),
        :client => current_user.client %>
<% end %>
<br/>
