Hello, <%= @user.name %>!

As you need to reset password for your account please click on following link to setup new password:

  <%= url(:reset_password, :token => @user.password_reset_token) %>

Rubytime
<%= @url %>