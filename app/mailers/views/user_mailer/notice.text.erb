Hello <%= @user.name %>,

You have missed fill activities in your callendar on:

<% @missed_days.each do |missed_day| %>
  <%= missed_day %>
<% end %>

This message has been sent automatically.

Rubytime
<%= @url %>